services:
  meu-site:
    build: .
    container_name: meu-site-portfolio
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
